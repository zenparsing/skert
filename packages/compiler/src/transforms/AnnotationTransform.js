export function registerTransform() {
  // Annotations are not included in the tree and therefore will not be included
  // in the output. Eventually we'll want to provide support for printing trees
  // that contain annotations. In that case we'll need to clear annotations
  // here.
}
